# Appwrite Boundary

Browser-safe and server-only Appwrite clients will live here. Server/admin SDK
clients must be initialized lazily and must never expose server API keys through
`NEXT_PUBLIC_*` variables.
